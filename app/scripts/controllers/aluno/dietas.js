'use strict';

/**
 * @ngdoc function
 * @name gainApp.controller:dietasCtrl
 * @description
 * # MainCtrl
 * Controller of the gainApp
 */
angular.module('gainApp')
  .controller('dietasCtrl', function($scope, $http, Alunos, $window) {
  });
