/**
 * @ngdoc function
 * @name gainApp.controller:cadTreinoCtrl
 * @description
 * # cadAlunoCtrl
 * Controller of the gainApp
 */
angular.module('gainApp')
  .controller('treinoCtrl', function($scope, $http, Alunos, Treinos, $window) {

  });
